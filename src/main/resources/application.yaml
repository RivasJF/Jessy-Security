spring:
  application:
    name: Jessy Security
  datasource:
    url: jdbc:postgresql://localhost:5432/node_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true


server:
  port: 3000
  servlet:
    context-path: /api

